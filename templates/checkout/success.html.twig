{% extends 'base.html.twig' %}
{% block title %}Paiement réussi{% endblock %}

{% block body %}
<div class="container py-5">

  <div class="alert alert-success text-center">
    <h3>Paiement confirmé ✅</h3>
    <p>Merci {{ app.user.firstname ?? app.user.email }} pour votre commande !</p>
  </div>

  {% if order is defined %}
    <div class="card shadow-sm my-4">
      <div class="card-header bg-success text-white">
        <h5 class="mb-0">Récapitulatif de la commande #{{ order.id }}</h5>
      </div>
      <div class="card-body">
        <p><strong>Date :</strong> {{ order.createdAt|date('d/m/Y H:i') }}</p>
        <p><strong>Statut :</strong> 
          {% if order.status == 'paid' %}
            <span class="badge bg-success">Payée</span>
          {% else %}
            <span class="badge bg-warning text-dark">{{ order.status }}</span>
          {% endif %}
        </p>

        <table class="table table-striped">
          <thead>
            <tr>
              <th>Produit</th>
              <th>Quantité</th>
              <th>Prix unitaire</th>
              <th>Total</th>
            </tr>
          </thead>
          <tbody>
            {% for item in order.items %}
              <tr>
                <td>{{ item.product ? item.product.name : 'Produit supprimé' }}</td>
                <td>{{ item.quantity }}</td>
                <td>{{ (item.unitPriceCents / 100)|number_format(2, ',', ' ') }} €</td>
                <td>{{ (item.lineTotalCents / 100)|number_format(2, ',', ' ') }} €</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>

        <div class="text-end fw-bold">
          Total payé : {{ (order.totalCents / 100)|number_format(2, ',', ' ') }} €
        </div>
      </div>
    </div>
  {% else %}
    <div class="alert alert-info">
      Votre commande a bien été enregistrée, mais aucun détail n’a été trouvé.
    </div>
  {% endif %}

  <div class="text-center mt-4">
    <a class="btn btn-primary" href="{{ path('app_products') }}">
      🛍️ Retour à la boutique
    </a>
  </div>
</div>
{% endblock %}
